[build-system]requires = ["setuptools>=64", "wheel"]build-backend = "setuptools.build_meta"[project]name = "pgxl-testkit"version = "0.2.0"requires-python = ">=3.10"dependencies = [  "typer>=0.12",  "pydantic>=2.7",  "pyyaml>=6.0",][project.scripts]pgxl-test = "pgxl_testkit.cli:app"pgxl-dev  = "pgxl_testkit.cli_devices:app"[tool.setuptools.packages.find]where = ["src"]